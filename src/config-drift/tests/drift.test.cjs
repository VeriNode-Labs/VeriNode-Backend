// NOTE: this is a lightweight smoke test intended for quick local validation.
// It does not run in Jest/ts-node due to environment constraints; it is kept
// as CommonJS and avoids importing TypeScript directly.

console.log('config-drift smoke-test: skipped (TypeScript import not available in CJS without ts-node registration)');

